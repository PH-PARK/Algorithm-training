#include <stdio.h>
#include <stdlib.h>

typedef struct node
{
	int key;
	struct node *leftChild, *rightChild;
}treeNode;


int main(void)
{
	treeNode* A;
	A->leftChild = NULL;
	A->rightChild = NULL;

	int i, num

}

inser_BST(int num);